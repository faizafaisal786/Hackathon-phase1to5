# OpenAI API Key (Optional - can use demo mode)
# For FREE demo mode (no API key required), set this to "demo"
# For full AI features, use your OpenAI API key
OPENAI_API_KEY=demo

# Note: In demo mode, the app uses simple pattern matching instead of OpenAI API
# This allows you to test and use the app without any API costs!
